# Core dependencies of RDL, exported from the authors' conda env (Python 3.10) and pruned.
# Install PyTorch first, e.g. CUDA 12.8 build:
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
torchvision==0.23.0

# Model backbones
timm==1.0.21
transformers==4.57.3
einops==0.8.1
open_clip_torch==3.3.0
# OpenAI CLIP is installed separately (see README): its setup.py imports pkg_resources,
# so it must be built with --no-build-isolation against setuptools<81 below.
setuptools<81
kornia==0.8.2

# SAFE / AIDE specific
pytorch-wavelets==1.3.0
PyWavelets==1.8.0
torchsde==0.2.6
torchdiffeq==0.2.5

# Data & video processing
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
scikit-image==0.25.2  # required by vidaug but not declared by it
opencv-python==4.12.0.88
Pillow==11.3.0
decord==0.6.0
moviepy==2.2.1
imageio==2.37.0
imageio-ffmpeg==0.6.0
scikit-video==1.1.11
albumentations==2.0.8
vidaug==1.5
tqdm==4.70.0

# Experiment framework
hydra-core==1.3.2
omegaconf==2.3.0
easydict==1.13  # required by VideoMAEv2's remote code (OpenGVLab/VideoMAEv2-*)
loguru==0.7.3
tabulate==0.9.0
torchinfo==1.8.0
tensorboard==2.20.0
PyYAML==6.0.3

# Analysis & plotting (data_analysis/)
matplotlib==3.10.7
seaborn==0.13.2
